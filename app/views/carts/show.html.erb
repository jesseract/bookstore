<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h2>Your cart</h2>
<ul>
  <% @cart.line_items.each do |book| %>
    <li><%= item.book.title %></li>
  <% end %>
  </ul>

<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
